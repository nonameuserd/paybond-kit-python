version: 1
name: strict-agent-v1
default_deny: true

tools:
  travel.book_hotel:
    side_effecting: true
    max_spend_cents: 1000
    evidence_preset: cost_and_completion

intent:
  allowed_tools:
    - travel.book_hotel
  budget:
    currency: usd
    max_spend_usd: 10
