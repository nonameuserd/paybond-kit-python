version: 1
name: shopping-agent-v1
default_deny: true

tools:
  commerce.checkout:
    side_effecting: true
    evidence_preset: cost_and_completion

  search.products:
    side_effecting: false

intent:
  allowed_tools:
    - commerce.checkout
